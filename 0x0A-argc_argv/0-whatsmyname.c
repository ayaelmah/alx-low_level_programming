#include <stdio.h>

/**
 * main - a stupid function
 * @argv: argument vector
 * @argc: count of arguments
 * Return: returns 0 if right
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
